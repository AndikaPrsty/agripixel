# Memory

## Project Overview
See @README.md for project overview and @package.json for available npm/pnpm commands for this project.

## Code Style Guidelines
- Use descriptive variable names
- Follow existing patterns in the codebase
- Extract complex conditions into meaningful boolean variables

## Architecture Notes
Add important architectural decisions and patterns here.

## Placeholder Assets (to be removed)
- `raw-assets/preload{m}/logo.svg` — still referenced by `LoadScreen.ts` (splash logo), keep for now.
- `raw-assets/main{m}/logo-white.svg` — was a placeholder, deleted (2026-08-19). Originally consumed by `src/app/screens/main/Logo.ts` (used by `Bouncer`), which was also removed the same day.

## Common Workflows
Document frequently used workflows and commands here.
